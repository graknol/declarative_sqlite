{
  "name": "declarative-sqlite-ts",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript port of declarative_sqlite - monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "keywords": [],
  "author": "graknol",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
